// A `for` loop walks a RANGE, giving you one value at a time. `1..=10` means the
// sequence 1, 2, 3, ... 10 (the `=` includes the end). Print a multiplication table:
// for each i from 1 to 10, print  number x i = result.

fn main() {
    let number = 5;

    // Loop i over 1..=10 and print  "{number} x {i} = {number*i}".
    // your code here

    // Expected:
    //   5 x 1 = 5
    //   5 x 2 = 10
    //   ...
    //   5 x 10 = 50
}
