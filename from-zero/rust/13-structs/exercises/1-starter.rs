// A struct bundles several named values into one custom type.
struct Point {
    x: i32,
    y: i32,
}

fn main() {
    // Create a Point with x = 3 and y = 4, then print both fields.
    // (create: Point { x: ..., y: ... }   access: p.x)
    // your code here

    println!("{} {}", p.x, p.y);   // should print: 3 4
}
