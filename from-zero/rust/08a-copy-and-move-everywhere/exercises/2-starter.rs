fn main() {
    let start = 3;
    let width = 4;

    for i in start..start + width {
        // your code here: print i
    }

    // your code here: print  start = {start}, width = {width}
    // (both are still usable — the range only copied their values in)
}
