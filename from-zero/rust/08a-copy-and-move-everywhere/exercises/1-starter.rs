fn main() {
    let n: usize = 4;

    let mut sum = 0;
    for _ in 0..n {
        // your code here: add 1 to sum each pass
    }

    // your code here: print  looped {n} times, sum = {sum}
    // (this line proves n is still usable after the loop)
}
