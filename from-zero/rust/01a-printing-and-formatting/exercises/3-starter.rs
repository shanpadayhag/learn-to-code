// Exercise 3 — debug a pair.
//
// A tuple has no plain `{}` display, but it has a DEBUG view.
// Put two numbers in a tuple and print the whole tuple with `{:?}`.
//
// Expected output:
//   (3, 4)
//
// Run it:  rustc 3-starter.rs && ./3-starter

fn main() {
    let point = (3, 4);
    // print `point` using the debug specifier
}
