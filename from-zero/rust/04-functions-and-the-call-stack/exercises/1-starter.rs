// Exercise 1 — a square machine.
//
// Fill in the body of `square` so it returns n * n.
// (The last line with no semicolon is what the function hands back.)
// It should print: 25
//
// Run it:  rustc 1-starter.rs && ./1-starter

fn square(n: i32) -> i32 {
    // your code here
}

fn main() {
    let result = square(5);
    println!("{result}");
}
